{
  "name": "skdjflksdf",
  "version": "1.0.0",
  "description": "Run `remixd` in this folder to then connect it to the remix IDE on ethereum.org.",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "compile": "bash run-compile-smtcheck.sh"
  },
  "author": "",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@openzeppelin/contracts": "^4.8.3",
    "solc": "^0.8.19"
  }
}
